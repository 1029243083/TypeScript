export enum chessType {
    none,
    red,
    blak
}

export enum GameState {
    gameing,
    redWin,
    blakWin,
    equal
}

export enum nextChess {
    red = chessType.red,
    blak = chessType.blak
}